import { FiltroLocacionPipe } from './filtro-locacion.pipe';

describe('FiltroLocacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroLocacionPipe();
    expect(pipe).toBeTruthy();
  });
});
